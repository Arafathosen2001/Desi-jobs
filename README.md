1#What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

Answer: getElementById =captured 1 element with a specific id,,getElementsByClassName=Captures all matching elements with a class name.
querySelector=Captures first matching elements with a CSS selector,,,querySelectorAll=Captures All matching elements with a CSS selector

2. How do you create and insert a new element into the DOM?

Answer: Atfirst captured parent element where push element,,then create a element (document.createElement()),then (parent.appendChild())

3. What is Event Bubbling? And how does it work?

Answer: Event Bubbling is a process where when an event it is going to up 

4. What is Event Delegation in JavaScript? Why is it useful?

Answer: Event Delegation is a proces where place a event listener on the parent element and handle the event using bubbling.

5.What is the difference between preventDefault() and stopPropagation() methods?

Answer: Stop Event Bubbling

